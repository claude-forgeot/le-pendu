# Le Pendu (Hangman) - Dependencies
# Install with: pip install -r requirements.txt

# Pygame Community Edition - Graphics, audio, and input handling
# Used for: All UI views, sound effects, music playback
pygame-ce>=2.5.0

# OpenCV Python - Computer vision and video processing
# Used for: Video playback in win/lose sequences (hard mode)
opencv-python>=4.8.0
